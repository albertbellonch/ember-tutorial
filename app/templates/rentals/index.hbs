{{#list-filter
   filter=(action 'filterByCity')
   as |rentals|}}
  <ul class="results">
    {{#each rentals as |rental|}}
      <li>{{rental-listing rental=rental}}</li>
    {{/each}}
  </ul>
{{/list-filter}}
{{outlet}}
